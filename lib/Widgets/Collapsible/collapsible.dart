import 'package:flutter/cupertino.dart';

class Collapseable extends StatelessWidget{
  @override
  Widget build(BuildContext context) {    
    return CustomScrollView(
      slivers: <Widget>[
        SliverList(delegate: null,
          
        )
      ],
    );
  }
  
}